# Newsletter backend gate

The newsletter form is currently UI-only. Before enabling submission, connect it to a consent-aware backend that stores subscription timestamp/source, validates email server-side, provides unsubscribe handling, rate-limits abuse, and aligns with the store privacy/data-treatment policy.
