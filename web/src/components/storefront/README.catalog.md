# Catalog preservation rule

The existing Commerce API/fallback selection logic remains the source of products during this refactor. The new catalog components are presentation-only. Do not reduce the remote catalog to the eight local fallback records and do not hardcode product availability into the UI.
