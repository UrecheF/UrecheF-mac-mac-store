# Store location

The approved design reference uses `Magangué, Bolívar - Colombia`, while the legacy storefront code contains `Talaigua Nuevo, Bolívar, Colombia`. The new centralized configuration currently follows the approved design reference. This must be explicitly verified before release so the public site does not publish conflicting business addresses.
