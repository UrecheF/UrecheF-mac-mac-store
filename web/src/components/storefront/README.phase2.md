# Phase 2 — validation + integration

Validate branch compilation, mount the new component tree into App.jsx while deleting replaced legacy markup, run build/lint again, and perform desktop/mobile visual review. Keep the PR draft until this phase is complete.
