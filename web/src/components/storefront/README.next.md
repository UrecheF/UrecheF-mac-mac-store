# Next integration milestone

The next code change should be deliberately small: mount the stable storefront components into `App.jsx` and remove the legacy duplicated service/contact/footer markup in the same change. Do not append the new footer beneath the old footer.

After that integration, run CI/build and compare the branch visually with the approved design before any merge.
