# Carousel administration contract

The UI layer is ready for an admin source to provide banner records. A future admin screen/API should create, edit, activate/deactivate, reorder, upload/select media, and associate a banner with a product/URL. The storefront itself must remain read-only and consume only published banners.
