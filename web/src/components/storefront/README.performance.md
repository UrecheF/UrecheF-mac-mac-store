# Performance baseline

Real product images use native lazy loading and async decoding. The new UI does not add additional animation libraries or client-side data dependencies. CDN image sizing/formats, Core Web Vitals measurement, and route-level code splitting remain release optimization tasks.
