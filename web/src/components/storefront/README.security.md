# Frontend security baseline

No secrets are introduced by this storefront work. External links opened in new tabs use `rel="noreferrer"`; WhatsApp text is URL-encoded; newsletter persistence is disabled until a server endpoint exists; and the future AI integration is explicitly server-side. A broader CSP/security-header review belongs to deployment hardening.
