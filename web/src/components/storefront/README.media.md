# Product media migration

The new product media component is ready for HTTPS/CDN image URLs. The next backend/catalog change should provide image URLs per product rather than embedding large image data in frontend source. Until real media is supplied, the component renders a branded fallback instead of fabricating product photography.
