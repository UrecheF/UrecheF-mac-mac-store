# Production state

This feature branch does not modify `.github/workflows/deploy-web.yml`, `web/src/App.jsx`, or the production branch. GitHub Pages deployment from `main` therefore remains on the existing storefront until an approved merge occurs.
