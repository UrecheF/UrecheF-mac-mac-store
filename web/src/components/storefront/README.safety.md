# Production safety boundary

The expansion has an error boundary available for integration. More importantly, it is still isolated from the live `App.jsx`; therefore the current production storefront cannot be affected by these branch-only components.
