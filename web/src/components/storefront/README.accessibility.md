# Accessibility baseline

New interactive controls include explicit accessible labels where icon-only, external links use safe rel attributes, the newsletter input has a real label, semantic sections/articles are used, and the carousel controls are keyboard-native buttons. Full keyboard/contrast review remains part of visual validation.
