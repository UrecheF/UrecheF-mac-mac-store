# WhatsApp routing rule

All new storefront WhatsApp CTAs are generated through `createWhatsAppUrl` in `src/config/store.js`, keeping the phone number in one place and encoding context-specific messages for sales, quotes, support, post-sale, technical service, and product interest.
